//ESCOPO: trecho de codigo, limitado por chaves.


let z = 5;

function soma(x, y) {
    return x + y + z;
}//tudo que é declarado dentro de uma função(escopo) so existe dentro da função.

console.log(soma(5, 3));